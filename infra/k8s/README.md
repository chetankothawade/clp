# Kubernetes deployment notes

This folder is reserved for future Kubernetes manifests and deployment templates.

Suggested folders:
- `deploy/`
- `config/`
- `secrets/`

Use one deployment per service, with a shared ingress and service discovery layer.
